---
source: generated by `linkbreakers gendocs`
---

## linkbreakers links create

Create a link

```
linkbreakers links create [flags]
```

### Examples

```
  linkbreakers links create --destination https://example.com
  linkbreakers links create --destination https://example.com --name "Launch" --shortlink launch
  linkbreakers links create --destination https://example.com --tags campaign-a,campaign-b --metadata owner=marketing

```

### Options

```
      --conversion-tracking           Preserve lbid during redirects.
      --custom-domain-id string       Custom domain ID.
      --destination string            Destination URL.
      --directory-id string           Directory ID.
      --fallback-destination string   Fallback destination URL.
  -h, --help                          help for create
      --metadata stringArray          Metadata in key=value form. Repeatable.
      --name string                   Optional display name.
      --qrcode-design-id string       QR code design ID.
      --qrcode-template-id string     QR code template ID.
      --shortlink string              Optional shortlink path.
      --tags string                   Comma-separated tags.
      --wait-for-qrcode               Wait for QR code generation.
```

### Options inherited from parent commands

```
      --base-url string   API base URL. Overrides config and LINKBREAKERS_BASE_URL.
  -o, --output string     Output format: json or table.
      --token string      API token. Overrides config and LINKBREAKERS_TOKEN.
```

### SEE ALSO

* [linkbreakers links](linkbreakers_links.md)	 - Manage links

###### Auto generated by spf13/cobra on 1-Apr-2026
